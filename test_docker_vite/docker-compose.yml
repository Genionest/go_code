version: '3.8'
services:
  vite-app:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - ./src:/app/src   # 实时同步本地代码更改
      - /app/node_modules # 保持容器内依赖独立
    environment:
      - CHOKIDAR_USEPOLLING=true # 解决文件监视问题